# FastAPI and web server dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# CORS and web middleware
python-multipart>=0.0.6
aiofiles>=23.2.1

# Core ML and RAG dependencies
# PyTorch with CUDA support - install from PyTorch index
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0+cu121
torchvision>=0.16.0+cu121
torchaudio>=2.1.0+cu121
transformers>=4.35.0
sentence-transformers>=2.2.2
ollama>=0.1.7
pandas>=2.1.0
numpy>=1.24.0
tqdm>=4.66.0
pyarrow>=10.0.0  # Required for parquet file support

# Vector search and indexing
faiss-cpu>=1.7.4  # Vector search (GPU acceleration handled by PyTorch)
# Note: faiss-gpu not available on Windows via pip, use faiss-cpu with CUDA PyTorch
rank-bm25>=0.2.2  # BM25 ranking for hybrid retrieval

# Additional ML utilities
scikit-learn>=1.3.0
scipy>=1.11.0

# HTTP client for testing and health checks
requests>=2.31.0
httpx>=0.24.0

# Optional: Performance improvements
# uvloop>=0.19.0  # Unix only - uncomment for better async performance
# httptools>=0.6.1  # Faster HTTP parsing

# Development and debugging
rich>=13.0.0  # Better console output
loguru>=0.7.0  # Enhanced logging

# Environment management
python-dotenv>=1.0.0  # Load environment variables from .env file
